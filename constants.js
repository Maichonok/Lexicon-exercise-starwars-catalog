const characterList = document.querySelector(".character-list");
const characterInfo = document.querySelector(".character-info");
const planetInfo = document.querySelector(".planet-info");
const characterName = document.querySelector(".header-name");
const planetName = document.querySelector(".header-planet");
const loaderDetails = document.getElementById("loader-details");
const nextButton = document.querySelector(".next-button");
const prevButton = document.querySelector(".prev-button");
const listCharacters = document.querySelector(".character-list");

export {
  characterList,
  characterName,
  characterInfo,
  planetName,
  planetInfo,
  nextButton,
  prevButton,
  loaderDetails,
  listCharacters,
};
